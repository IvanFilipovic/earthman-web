import gsap from "gsap";
import { vGsapDirective } from "./plugin.js";
export const vGsapVue = (configOptions) => {
  return vGsapDirective(
    "vue",
    configOptions || {},
    gsap.context(() => {
    }),
    null
  );
};
